apiVersion: v1
kind: Service
metadata:
  name: {{ include "skbank-common.fullname" . }}
  labels:
    {{- include "skbank-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skbank-common.selectorLabels" . | nindent 4 }}
