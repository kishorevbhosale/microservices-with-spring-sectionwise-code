apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skbank-common.fullname" . }}-test-connection"
  labels:
    {{- include "skbank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skbank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
