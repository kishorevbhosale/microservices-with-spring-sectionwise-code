apiVersion: v1
kind: Service
metadata:
  name: {{ include "skbank-test.fullname" . }}
  labels:
    {{- include "skbank-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skbank-test.selectorLabels" . | nindent 4 }}
