apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skbank-test.fullname" . }}-test-connection"
  labels:
    {{- include "skbank-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skbank-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
